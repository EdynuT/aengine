plugins {
    id 'java-library'
    id 'application'
    id 'maven-publish'
}

group   = 'com.aengine'
version = '0.11.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

repositories {
    mavenCentral()
}

ext {
    lwjglVersion = '3.3.4'
    jomlVersion  = '1.10.5'
    imguiVersion = '1.86.11' 
    
    String osName = System.getProperty("os.name").toLowerCase()
    if (osName.contains("win")) {
        lwjglNatives = 'natives-windows'
        imguiPlatform = 'natives-windows'
    } else if (osName.contains("mac")) {
        lwjglNatives = 'natives-macos'
        imguiPlatform = 'natives-macos'
    } else {
        lwjglNatives = 'natives-linux'
        imguiPlatform = 'natives-linux'
    }
}

dependencies {
    // Core Engine Bindings
    implementation "org.lwjgl:lwjgl:${lwjglVersion}"
    implementation "org.lwjgl:lwjgl-glfw:${lwjglVersion}"
    implementation "org.lwjgl:lwjgl-opengl:${lwjglVersion}"
    implementation "org.lwjgl:lwjgl-stb:${lwjglVersion}"
    api "org.joml:joml:${jomlVersion}"

    // Native Platforms Resolvers
    runtimeOnly "org.lwjgl:lwjgl:${lwjglVersion}:${lwjglNatives}"
    runtimeOnly "org.lwjgl:lwjgl-glfw:${lwjglVersion}:${lwjglNatives}"
    runtimeOnly "org.lwjgl:lwjgl-opengl:${lwjglVersion}:${lwjglNatives}"
    runtimeOnly "org.lwjgl:lwjgl-stb:${lwjglVersion}:${lwjglNatives}"

    // Json parsing library for configuration and serialization
    implementation 'com.google.code.gson:gson:2.10.1'
    
    // Embedded Lua Virtual Machine
    implementation 'org.luaj:luaj-jse:3.0.1'
}

application {
    mainClass = 'com.aengine.Main'
}

run {
    jvmArgs += [
        '--enable-native-access=ALL-UNNAMED',
        '--add-opens', 'java.base/java.lang=ALL-UNNAMED',
        '--add-opens', 'java.base/java.nio=ALL-UNNAMED'
    ]
}

java {
    withSourcesJar()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            pom {
                name = 'AEngine'
                description = 'Java 2D/3D graphics engine (OpenGL 3.3 backend)'
            }
        }
    }
    repositories {
        mavenLocal()
    }
}

tasks.named('run') {
    standardInput = System.in
}
